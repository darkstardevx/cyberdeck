#[derive(Clone)]
pub struct AppState {
    // Add shared fields here, for example:
    // pub hardware_info: String,
    // pub db_pool: MyDbPool,
}

impl AppState {
    pub fn new() -> Self {
        Self {
            // Initialize your fields here
        }
    }
}
